window.addEventListener("load", inicializar, false);
const formulario = document.querySelector('examenForm');
let respuesta1 = "París";
pregunta1.addEventListener("click", myFunction);

myFunction(){
    
}